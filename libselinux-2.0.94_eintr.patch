diff -up libselinux-2.0.94/src/avc_internal.c.eintr libselinux-2.0.94/src/avc_internal.c
--- libselinux-2.0.94/src/avc_internal.c.eintr	2010-03-24 15:52:20.000000000 -0400
+++ libselinux-2.0.94/src/avc_internal.c	2012-02-03 10:45:37.308400755 -0500
@@ -101,7 +101,9 @@ static int avc_netlink_receive(char *buf
 	socklen_t nladdrlen = sizeof nladdr;
 	struct nlmsghdr *nlh = (struct nlmsghdr *)buf;
 
-	rc = poll(&pfd, 1, (blocking ? -1 : 0));
+	do {
+		rc = poll(&pfd, 1, (blocking ? -1 : 0));
+	} while (rc < 0 && errno == EINTR);
 
 	if (rc == 0 && !blocking) {
 		errno = EWOULDBLOCK;
